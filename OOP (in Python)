>>> means command, followed by output
# means comment

#Basically, object x has attributes (x.attribute) and (methods x.method). These can be changed independently of each other. The changes apply to x independently of other objects.

#Examples:
#hw4-skeleton/Q2
#math/AOPS/Checkers.ipynb

#code
class Name(object):
	attribute=value
	def __init__(self, input):
		self.attribute2=f(input, attribute)
	def method(self, input2):
		self.attribute2=f(input2, attribute)

#result
>>> x=Name()
>>>x.attribute
value
>>> x.attribute2
f(input, value)
>>> x.method(input2, value)
>>>x.attribute2
f(input2)